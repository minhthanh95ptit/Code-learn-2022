package sequence;

// Construct an array b of prefix sums of the given array a.
// b is defined as:

//   b[0]   = a[0]
//   b[1]   = a[0] + a[1]
//   b[2]   = a[0] + a[1] + a[2]
//   ...
//   b[n - 1] = a[0] + a[1] + ... + a[n - 1]
// where n is the length of a.

public class _3_prefixSums {
    static int[] prefixSums(int[] a) {
        return a;
    }
    public static void main(String[] args) {
        
    }    
}
